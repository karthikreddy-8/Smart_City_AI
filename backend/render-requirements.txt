# ─────────────────────────────────────────────────────────────────────────────
# render-requirements.txt
# Use this file for Render deployment to avoid downloading YOLO (200MB+).
# In Render dashboard: Settings → Build Command:
#   pip install -r render-requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.6

passlib[bcrypt]==1.7.4
bcrypt==4.0.1

python-jose[cryptography]>=3.3.0
python-multipart>=0.0.6

pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.2.0
xgboost>=1.7.0
joblib>=1.3.0
openpyxl>=3.1.0
reportlab>=4.0.0
jinja2>=3.1.2
python-dotenv>=1.0.0
requests>=2.31.0
email-validator

# NOTE: ultralytics and opencv-python-headless are intentionally excluded.
# YOLO is disabled on Render (USE_YOLO env var is not set to 'true').
# Add them back only if you enable YOLO on a paid Render instance.
